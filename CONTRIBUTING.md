# 参与开发

本仓库只接受直接服务于 AI8video 的改动。

## 边界

- Web、CLI、Electron、标准模式与 Agent 模式必须复用项目自有的 Python Runtime 和领域服务。
- Python 是对话、任务状态、业务规则、凭据来源、成本、媒体处理和持久化的唯一真值来源。
- Pi 等第三方 Agent 依赖只能放在受控适配层后，用于模型与工具轮次；不得接管产品会话、任务账本或业务规则。
- 不向模型开放 Shell、任意文件系统、浏览器、代码执行或通用网络工具。
- 不建立第二套聊天前端、生成流水线、TTS、知识库、归档或独立 Agent 身份。
- 第三方依赖必须锁定版本并核对许可证；仓库不得提交 FFmpeg、模型、浏览器或其他大型二进制。

## 提交前检查

1. 只运行与改动直接相关的最小单元测试，不用全量回归代替范围判断。
2. 按改动类型运行必要的 Python 编译检查、Node 语法检查、Shell 语法检查或 Markdown 链接检查。
3. 模型与媒体测试必须处于 dry-run（试运行）或使用显式测试替身，不得消耗真实额度。
4. PostgreSQL 集成测试只能使用独立、可丢弃的测试数据库。
5. 提交前检查暂存内容，确保没有 `.env`、`mykey.py`、本机配置目录、用户素材、生成结果、密钥或登录状态。
6. 新增依赖时同步更新 `THIRD_PARTY_NOTICES.md`。

示例最小测试：

```bash
AI8VIDEO_DISABLE_MYKEY=1 AI8VIDEO_DRY_RUN=1 PYTHONPATH=src \
  python -m unittest tests.test_ai8video_architecture
```

## 发布提交说明

推送面向用户的更新时，提交标题应概括本轮升级，提交正文必须写清：

- 主要功能或运行态变化；
- UI、文档、兼容性和清理项；
- 实际执行过的测试、构建与冒烟验证；
- 已知边界或未包含的运行时。

稳定用法和长期边界同步更新 README；本轮更新的详细说明放在 Git 提交正文中，不用 README 代替提交说明。

创建正式版本时必须使用 `git tag -a` 注解标签，标签正文沿用上述模块化结构；桌面发布工作流会把标签正文作为 GitHub Release 更新说明，不得只留下自动生成的 Full Changelog 链接。
